.c-blogroll__link {
  text-decoration: none;

  &:visited {
    color: var(--color-text);
  }
}

.c-blogroll__link--feed-url {
  display: inline-flex;
}

.c-blogroll__link--feed-url svg {
  flex: 1 0 auto;
  width: 1em;
  height: 1em;
  margin-top: auto;
  margin-right: 0.25em;
  margin-bottom: auto;
  fill: currentColor;
}
